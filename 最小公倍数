#include <stdio.h>
int main()
{
	int num1 = 0;
	int num2 = 0;
	int tmp = 0;
	scanf("%d%d", &num1, &num2);
	if (num1 < num2)
	{
		tmp = num1*(num2%num1);
	
	}
	else
	{
		tmp = num2*(num1%num2);
	}
	printf("%d ", tmp);
  return 0;
}
