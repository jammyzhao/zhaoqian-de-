#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
int main()
{
	int a, b;
	int m = 0;
		printf("请输入a,b");
	scanf_s("%d%d", &a, &b);
	if (a >= b)
		m =a;
	   else 
		   m = b;
	printf("a,b中最大值为 %d\n",m);
	return 0;
}
